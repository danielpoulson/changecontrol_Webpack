import React, { Component } from 'react';
/* component styles */
import { styles } from './styles.scss';

export const Home = () => {

    return (
      <section>
        <div className={`${styles}`}></div>
      </section>

    );
}
