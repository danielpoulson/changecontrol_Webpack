import React, { Component } from 'react';
/* component styles */
import { styles } from './styles.scss';

export const Home = () => (
  <section>
    <div className={`${styles}`}></div>
  </section>

);
